import { JustForAdminDirective } from './just-for-admin.directive';

describe('JustForAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new JustForAdminDirective();
    expect(directive).toBeTruthy();
  });
});
